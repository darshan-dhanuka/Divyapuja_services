/**
 * Add cart item
 */
module.exports.addCartItem = (dataObj) => {
  console.log("from cartitem model")
  //   return  connection.query('INSERT INTO tbl_cart_products (product_id, user_id,shipping ,expected_delivery_date ) VALUES ("'+user["body"]["product_id"]+'","'+user["body"]["user_id"]+'","'+user["body"]["shipping"]+'","'+user["body"]["expected_delivery_date"]+'")', (err, row) => {
  //       console.log(err);
  //           cb(err, row)
  //   });
    
  // return categories.create(catObj)
  //   .then(catDetails => catDetails.id)
  //   .catch(error => {
  //     console.log(error);
  //     return false;
  //   });
}


// module.exports.cartItemModel = cartItemModel;
